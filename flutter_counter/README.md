# Exercise B — Flutter Widget Test

Files created:

- `pubspec.yaml` — package config
- `lib/counter.dart` — `CounterWidget` implementation
- `test/counter_widget_test.dart` — widget test for increment/decrement

Run tests:

```bash
# from the flutter_counter folder
flutter test
```

Notes:

- Ensure Flutter SDK is installed and on PATH before running `flutter test`.
